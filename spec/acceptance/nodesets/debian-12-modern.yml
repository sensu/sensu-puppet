HOSTS:
  sensu-agent:
    roles:
      - agent
      - sensu-agent
    platform: debian-12-amd64
    hypervisor: docker
    image: debian:12
    docker_preserve_image: true
    docker_cmd:
      - '/sbin/init'
    docker_image_commands:
      - 'apt-get update'
      - 'apt-get install -y wget curl net-tools systemd-sysv locales ca-certificates iproute2'
      - 'echo "LC_ALL=en_US.UTF-8" >> /etc/environment'
      - 'echo "en_US.UTF-8 UTF-8" >> /etc/locale.gen'
      - 'echo "LANG=en_US.UTF-8" > /etc/locale.conf'
      - 'locale-gen en_US.UTF-8'
    docker_container_name: 'sensu-agent-debian12'
  sensu-backend:
    roles:
      - agent
      - sensu-backend
      - puppetserver
      - default
    platform: debian-12-amd64
    hypervisor: docker
    image: debian:12
    docker_preserve_image: true
    docker_cmd:
      - '/sbin/init'
    docker_image_commands:
      - 'apt-get update'
      - 'apt-get install -y wget curl net-tools systemd-sysv locales ca-certificates iproute2'
      - 'echo "LC_ALL=en_US.UTF-8" >> /etc/environment'
      - 'echo "en_US.UTF-8 UTF-8" >> /etc/locale.gen'
      - 'echo "LANG=en_US.UTF-8" > /etc/locale.conf'
      - 'locale-gen en_US.UTF-8'
    docker_container_name: 'sensu-backend-debian12'
CONFIG:
  log_level: debug
  type: foss
ssh:
  password: root
  auth_methods: ["password"]
